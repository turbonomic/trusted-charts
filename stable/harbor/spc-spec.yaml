requirements:
  ## Set nodes spec
  node: # {} if nothing set
    count: 3
    cpu: 2
    ram: 4GB

  ## Set app spec
  app:
    namespace: harbor
    storage: true
    loadbalancer: false
    ingress: true
    ## List of required values key names from values.yaml
    valuesEditRequired: [] # [] if nothing set
#      - some_key
