requirements:
  ## Set nodes spec
  node: # {} if nothing set
    count: 4
    cpu: 2
    ram: 8GB

  ## Set app spec
  app:
    rbac: false
    namespace: spinnaker
    storage: false
    loadbalancer: false

    ## List of required values key names from values.yaml
    valuesEditRequired: [] # [] if nothing set
#      - some_key
