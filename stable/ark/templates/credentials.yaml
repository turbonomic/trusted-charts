apiVersion: v1
kind: Secret
metadata:
  name: cloud-credentials
  namespace: {{ .Values.serverNamespace }}
  labels:
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
type: Opaque
data:
  credentials-ark: {{ .Values.cloudCredentials | b64enc | quote }}
