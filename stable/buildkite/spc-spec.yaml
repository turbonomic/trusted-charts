requirements:
  ## Set nodes spec
  node: {} # {} if nothing set
#    count: 2
#    cpu: 2
#    ram: 2GB

  ## Set app spec
  app:
    rbac: false
    namespace: buildkite
    storage: false
    loadbalancer: false

    ## List of required values key names from values.yaml
    valuesEditRequired: # [] if nothing set
      - agent.token
