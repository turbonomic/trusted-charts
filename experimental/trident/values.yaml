# Default values for trident.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: gcr.io/stackpoint-public/pitchfork
  tag: 0.1.0
  pullPolicy: IfNotPresent
