requirements:
  ## Set nodes spec
  node: # {} if nothing set
    count: 4
    cpu: 2
#    ram: 2GB

  ## Set app spec
  app:
    rbac: false
    namespace: kafka
    storage: true
    loadbalancer: false

    ## List of required values key names from values.yaml
    valuesEditRequired: [] # [] if nothing set
#      - some_key
